A *channel* is essentially a WebSocket-like interface. It is a two-way
communication medium. A *fetch* request can also be wrapped in a channel.
Communication between two worker threads can also be wrapped in a channel.
