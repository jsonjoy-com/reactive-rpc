import * as React from 'react';

export default {
  title: 'Clickable demo',
};

export const Default = {
  render: () => {
    return (
      <div>
        clickable demo...
      </div>
    );
  },
};
